// 

object Main extends App {
  val sc = new java.util.Scanner(System.in);
  val a, b, c = sc.nextInt();
  val t1 = sc.next();
  val t2 = sc.next();
  sc.nextLine(); // 行末スキップ
  val s2 = sc.nextLine();
  println(a + b + c);
  println(t1 + t2);
  println(s2);
}
